<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.oy.u911">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".application.TheApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 知乎日报 -->
        <activity
            android:name=".function.zhihu.ZhihuIndexActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".function.zhihu.StoryDetailActivity"
            android:screenOrientation="portrait"/>

        <!-- 干货集中营 -->
        <activity
            android:name=".function.gank.GankActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".function.gank.BrowserImageActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".function.gank.UrlBrowserActivity"
            android:screenOrientation="portrait"/>

        <!-- Dribbble -->
        <activity
            android:name=".function.dribbble.DribbbleActivity"
            android:screenOrientation="portrait"/>

    </application>

</manifest>